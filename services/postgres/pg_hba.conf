# PostgreSQL Host-Based Authentication (pg_hba.conf)
# Pure Bliss Development Environment - SSL/TLS Required

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Local connections (for maintenance)
local   all             all                                     scram-sha-256

# IPv4 connections - SSL REQUIRED for all remote connections
hostssl all             all             172.16.0.0/12           scram-sha-256
hostssl all             all             10.0.0.0/8              scram-sha-256
hostssl all             all             192.168.0.0/16          scram-sha-256

# Specific Pure Bliss network - SSL REQUIRED
hostssl all             all             samenet                 scram-sha-256

# Reject all non-SSL connections
host    all             all             all                     reject

# IPv6 connections - SSL REQUIRED
hostssl all             all             ::1/128                 scram-sha-256
